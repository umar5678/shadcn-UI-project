import Link from "next/link";

const Logo = () => {
  return (
    <Link href="/" className="flex items-center gap-2 font-bold text-xl">
      <span>ShadcnProject</span>
    </Link>
  );
};

export default Logo;
